# Training scalable feature-grid based INRs for images

## Overview

Script for training INRs on images. Input directory should contain set of images to train on independently.

Example run command for the Kodak dataset:

```
python3 app/image/main_image.py --config app/image/configs/kodak.yaml --dataset-path /path/to/kodak/directory
```
